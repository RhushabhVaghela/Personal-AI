[project]
name = "pai"
version = "0.1.0"
description = "Personal realtime voice + vision + computer-use AI assistant"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
