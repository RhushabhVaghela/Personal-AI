"""PersonalAI-Assistant — realtime voice + vision + computer-use assistant.

Entry points:
  python -m pai.terminal       — push-to-talk terminal loop
  python -m pai.server         — web dashboard (WebSocket UI)
"""
from . import config  # noqa: F401

__version__ = "0.1.0"
